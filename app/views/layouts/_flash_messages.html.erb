<% if flash.keys.any? %>
  <% flash.keys.each do |key| %>
    <div class="alert alert-<%= flash_helper(key)%>">
      <button type="button" class="close" data-dismiss="alert">
        <span aria-hidden="true">×</span>
        <span class="sr-only">X</span>
      </button>
      <%= flash[key] %>
    </div>
  <% end %>
<% end %>